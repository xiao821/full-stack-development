services:
  nuxt:
    build: .
    restart: always
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=your-secret-change-this
      - DB_PATH=/app/data/data.db
    volumes:
      - ./data:/app/data
